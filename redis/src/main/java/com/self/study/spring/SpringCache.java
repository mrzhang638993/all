package com.self.study.spring;

import com.self.study.redis.bo.User;
import org.springframework.cache.annotation.Cacheable;

public class SpringCache {



    //  指定cacheManager对应的缓存管理信息，cacheManager对应的是config的配置信息。cache-1对应的是缓存的位置信息，key是缓存的key
    //   其中构成的key信息可以如下：cache-1：userId构成完成的key的。

}
