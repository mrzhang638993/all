
1.不同的后缀名称，对应的是打开的不同的方式实现操作的。
有html，.htm方式的文件也是可以被浏览器程序打开的，对应的html开始的文件都是可以被浏览器打开的。
浏览器本身对应的是一个软件的。

2.html对应的是文本标签语言的。

3.文件乱码操作信息如下：文件编写的字符集和浏览器显示的字符集需要保证一致的，否则的话，会存在很多问题的。
元数据的作用是告诉浏览器应该怎么样解析html操作的。
网页内容的话，对应的都是body的标签的信息的。


4.前端的相对路径的写法是如下的：<img  src="test.png">  //  对应的是相对路径信息的
<img  src="c:/test.png">  对应的是绝对路径信息的。 书写相关的路径推荐使用/来实现相关的路径的操作处理的。
使用\ 代表的对应的是存在特殊的转义的操作实现的。
百分比的操作是相对于父容器进行相关的操作实现的。img的高度和宽度的百分比对应的都是相对于父标签的信息的。



